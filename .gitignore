# operational data, never committed
data/
archive/
dist/
__pycache__/
*.pyc
.env
# local verification output
verify-out/

# editor/agent local config
.claude/
# deploy settings for ops/aws/deploy.sh (no credentials, but per-account values)
ops/aws/deploy.env
