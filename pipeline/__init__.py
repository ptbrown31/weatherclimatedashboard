"""Data pipeline for the weather tools site: fetch US government feeds on a
schedule, archive every forecast cycle, and write small JSON snapshots that the
static frontend reads. See README.md for the jobs and config surface."""
